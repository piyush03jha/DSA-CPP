//largest element in an array
#include <iostream>
using namespace std;

int main(){

    return 0;
}

// brute force approact should be to apply sorting and print the largest element
// time complexity O(nlogn)
//space complexity O(1)